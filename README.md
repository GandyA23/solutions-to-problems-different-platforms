# Solutions to problems from different platforms 

## Eecommended shortcuts

```cpp
// Defs
typedef long long ll;
typedef pair<int, int> ii;
typedef vector<ii> vii;
#define INF 1000000000

// memset's options
memset(arr, -1, sizeof arr);    // Fill the array with -1
memset(arr, 0, sizeof arr);     // Fill the array with 0

// Shotcuts
i++;                            // i = i + 1;
ans = a ? b : c;                // if (a) ans = b; else ans = c;
ans += val;                     // ans = ans + val;
index = (index + 1) % n;        // index++; if (index >= n) index = 0;
index = (index + n - 1) % n;    // index--; if (index < 0) index = n-1;
ans = (int)((double) d + 0.5);  // Round to the nearest integer 
ans = min(ans, newValue);       // min/max
ans <?= newValue;               // min/max
```
